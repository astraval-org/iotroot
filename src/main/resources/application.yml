server:
    port: 8080

spring:
    datasource:
        url: jdbc:postgresql://203.57.85.21:5432/iotroot?serverTimezone=UTC&useUnicode=true&characterEncoding=UTF-8
        driver-class-name: org.postgresql.Driver
        username: iotrootuser
        password: IOTROOT25@db
        hikari:
            connection-init-sql: SET TIME ZONE 'UTC'
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
        database-platform: org.hibernate.dialect.PostgreSQLDialect
        properties:
            hibernate:
                jdbc:
                    time_zone: UTC

mqtt:
    broker:
        url: tcp://astraval.com:1883
        username: user1
        password: password1
        client-id: iotroot-backend
        topic: test/topic

logging:
    level:
        root: WARN
        com.astraval.iotroot: INFO

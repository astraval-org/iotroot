server:
    port: 8080

spring:
    datasource:
        url: jdbc:postgresql://203.57.85.21:5432/iotroot?serverTimezone=UTC&useUnicode=true&characterEncoding=UTF-8
        driver-class-name: org.postgresql.Driver
        username: iotroot_app
        password: av2025@IOTROOT
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
        database-platform: org.hibernate.dialect.PostgreSQLDialect
        properties:
            hibernate:
                jdbc:
                    time_zone: UTC

mqtt:
    broker:
        url: tcp://astraval.com:1883   # ssl://astraval.com:8883
        username: testuser
        password: password1
        client-id: iotroot_app
        tclient-id: iotroot_backend_${random.uuid} 
        clean-session: false
        keep-alive: 60
        connection-timeout: 30
        automatic-reconnect: true
        topic: iotroot/#
        
        
logging:
    level:
        root: WARN
        com.astraval.iotroot: INFO
